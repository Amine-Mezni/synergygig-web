# SynergyGig - AI Project Handover & Future Instructions

**Last Updated**: April 1, 2026  
**Status**: Live MVP with REST API + Web Dashboard  
**For**: Future AI Agents / Developers

---

## PROJECT OVERVIEW

**SynergyGig** is a comprehensive **HR & Workforce Management Platform** built as a PIDEV capstone at Esprit School of Engineering (Tunisia). It combines traditional HR operations with gig-economy tools and real-time collaboration features.

- **Live URL**: http://benzaitsue.work.gd
- **Server**: 64.23.239.27 (Linode VPS)
- **Repository**: c:\Users\seji\Desktop\java\SynergyGig
- **Start Date**: March 30, 2026
- **Current Phase**: Foundation (Database Integration + Full CRUD next)

---

## CURRENT DEPLOYMENT STATUS

### What's Working ✓
- Web application running at **http://benzaitsue.work.gd**
- REST API endpoints responding with JSON
- Web dashboard with stats display (http://benzaitsue.work.gd/dashboard.html)
- Docker containers (Nginx + PHP-FPM) on port 80
- Simple PHP routing system with basic endpoints

### What's Not Yet Integrated ✗
- MySQL database (credentials exist but not connected)
- User authentication / JWT tokens
- Full CRUD operations
- Input validation
- Error handling middleware
- Real-time features
- File uploads

---

## TECH STACK

```
Frontend:
  - HTML5 + Vanilla JavaScript
  - CSS3 (Gradient styling)
  - Responsive design

Backend:
  - PHP 8.3
  - Nginx:alpine (reverse proxy)
  - Docker Compose (orchestration)
  - Simple routing (no framework yet)

Database:
  - MySQL 8.0 (finale_synergygig)
  
Infrastructure:
  - Linode VPS (64.23.239.27)
  - Port 80 (HTTP)
  - SSH access via Ed25519 key
```

---

## CRITICAL CREDENTIALS & CONFIGS

⚠️ **IMPORTANT**: These are in `config.properties`

```properties
# Database
db.url=jdbc:mysql://localhost:3307/finale_synergygig
db.user=seji
db.password=MORTALkombat9pd6S##E

# Server
server.host=64.23.239.27
server.ssh_user=seji

# SSH Key
SSH_KEY=~/.ssh/id_ed25519
SSH_PASSPHRASE=u7@9pd6S##E

# API Keys (for future AI integrations)
gemini.api_key=AIzaSyC-...
rest.base_url=https://rest.benzaitsue.work.gd/api
```

---

## PROJECT STRUCTURE

```
SynergyGig/
├── public/
│   ├── index.php              # Router & main entry point
│   └── dashboard.html         # Web dashboard UI
├── src/
│   ├── Entity/
│   │   ├── User.php           # User/freelancer/manager model
│   │   ├── Offer.php          # Job posting model
│   │   ├── JobApplication.php # Application model
│   │   └── Contract.php       # Agreement model
│   ├── Repository/
│   │   └── OfferRepository.php# Database queries for offers
│   └── Controller/
│       ├── OfferController.php# Offer API endpoints
│       └── DashboardController.php # Dashboard data
├── docker/
│   └── nginx/
│       └── default.conf       # Nginx config
├── sql/
│   ├── offer_contract_tables.sql
│   └── training_tables.sql
├── Dockerfile                 # PHP 8.3 FPM image
├── docker-compose.yml         # Container orchestration
├── composer.json              # PHP dependencies
└── config.properties          # Database & API config
```

---

## REST API ENDPOINTS

### Current (Working)
```
GET  /                         - Welcome
GET  /api/health              - Health check  
GET  /api/dashboard           - Stats dashboard
GET  /api/offers              - List all offers
POST /api/offers              - Create offer
     Body: {title, type, amount, location}
```

### Needed (Priority Order)
```
# Users/Auth
POST   /api/auth/register      - User signup
POST   /api/auth/login         - User login
GET    /api/users/{id}         - User profile
PATCH  /api/users/{id}         - Update profile

# Applications
POST   /api/applications       - Apply to offer
GET    /api/applications       - List user's applications
PATCH  /api/applications/{id}  - Update app status

# Contracts
POST   /api/contracts          - Create contract
GET    /api/contracts          - List contracts
PATCH  /api/contracts/{id}     - Update contract

# Admin
GET    /api/admin/stats        - Full system stats
DELETE /api/offers/{id}        - Remove offer
```

---

## DEPLOYMENT PROCESS

### Files to Execute
All deployment done via Python scripts that SSH to the server:

```bash
# Current working scripts:
python build_rest_api.py      # Deploy API endpoints
python deploy_dashboard.py    # Deploy web UI
python fix_ports.py           # Expose port 80
python redeploy_fix.py        # Full redeploy
python deploy_minimal.py      # Clean deployment

# Server restart
ssh seji@64.23.239.27 "cd /home/seji/web_synergygig && docker compose restart"
```

### Adding New Features
1. **Create PHP files** in `src/`
2. **Upload via paramiko** in new Python script
3. **Update router** in `public/index.php`
4. **Restart containers** with docker compose

---

## ENTITY MODELS (Database Schema Needed)

### User
```php
id (PK)
email (UNIQUE)
password_hash
first_name
last_name
role (USER | FREELANCER | HR_MANAGER | ADMIN)
bio (TEXT, nullable)
created_at
```

### Offer
```php
id (PK)
title
description (TEXT, nullable)
offer_type (FREELANCE | FULL_TIME | PART_TIME | INTERNSHIP | CONTRACT)
status (DRAFT | OPEN | CLOSED | CANCELLED)
required_skills (TEXT, nullable)
location (nullable)
amount (DECIMAL)
currency (DEFAULT: USD)
owner_id (FK -> User)
start_date (nullable)
end_date (nullable)
created_at
```

### JobApplication
```php
id (PK)
applicant_id (FK -> User)
offer_id (FK -> Offer)
status (PENDING | ACCEPTED | REJECTED | WITHDRAWN)
cover_message (TEXT, nullable)
created_at
responded_at (nullable)
```

### Contract
```php
id (PK)
job_application_id (FK)
buyer_id (FK -> User)
freelancer_id (FK -> User)
status (DRAFT | ACTIVE | COMPLETED | CANCELLED | EXPIRED)
total_amount
start_date
end_date (nullable)
terms (TEXT, nullable)
milestones (TEXT, nullable)
created_at
signed_at (nullable)
```

---

## NEXT PRIORITIES FOR FUTURE AGENTS

### PHASE 1: Database Integration (Highest Priority)
**Goal**: Connect MySQL and run migrations  
**Tasks**:
1. ✓ Create SQL migration files (exist in sql/)
2. ✓ Connect to DB from PHP using PDO
3. ✓ Create Doctrine entity repository classes
4. ✓ Test CRUD operations

**Files to create**:
- `src/Database/Connection.php` - PDO connection class
- `src/Middleware/Database.php` - DB middleware

### PHASE 2: Full CRUD Endpoints
**Goal**: Make all API endpoints work with real database  
**Tasks**:
1. Create all controller methods
2. Add input validation
3. Implement proper error responses
4. Add pagination support

### PHASE 3: Authentication
**Goal**: Secure the API with JWT tokens  
**Tasks**:
1. Hash passwords (bcrypt)
2. Generate JWT tokens on login
3. Verify tokens on protected routes
4. Add role-based access control

### PHASE 4: Frontend Enhancements
**Goal**: Build interactive web UI  
**Tasks**:
1. Login/registration forms
2. Job listings with filters
3. Application submission
4. User dashboard
5. Admin panel

### PHASE 5: Advanced Features
**Goal**: Real-time & professional features  
**Tasks**:
1. WebSocket notifications
2. File upload system
3. Search & filtering
4. Email notifications
5. Analytics/reporting

---

## HOW TO ADD NEW FEATURES

### Example: Add Users Endpoint

**Step 1**: Create controller
```bash
# Send to server in Python script
controller_code = '''<?php
namespace App\Controller;
use Symfony\Component\HttpFoundation\JsonResponse;

class UserController {
    public function list() {
        // SQL query
        return new JsonResponse([...]);
    }
}
'''
```

**Step 2**: Update router in `public/index.php`
```php
'/api/users' => [$controller, 'list']
```

**Step 3**: Test endpoint
```bash
curl http://benzaitsue.work.gd/api/users
```

---

## DEBUGGING & TROUBLESHOOTING

### Check Server Status
```bash
python diagnose_https.py  # Full system check
```

### View Container Logs
```bash
ssh seji@64.23.239.27 "cd /home/seji/web_synergygig && docker compose logs -f"
```

### Restart Everything
```bash
ssh seji@64.23.239.27 "cd /home/seji/web_synergygig && docker compose down && docker compose up -d"
```

### Test Endpoint Locally
```bash
curl -s http://benzaitsue.work.gd/api/health | python -m json.tool
```

---

## IMPORTANT NOTES FOR FUTURE WORK

1. **Database is EMPTY** - Need to run SQL migrations first
2. **No Authentication Yet** - All endpoints currently public
3. **Simple Routing** - Using basic PHP (not a framework)
4. **Consider Framework Later** - Symfony/Laravel for scalability
5. **SSL/HTTPS Missing** - Only HTTP on port 80 (add Let's Encrypt)
6. **No Logging** - Should add logging middleware for debugging
7. **No Unit Tests** - Should add PHPUnit tests
8. **No API Documentation** - Should add Swagger/OpenAPI docs

---

## FOR NEXT AI AGENT

When continuing this project:

1. **Read this file first** ← You are here
2. **Check project state** - `memory/session/synergygig_project_state.md`
3. **View live dashboard** - http://benzaitsue.work.gd/dashboard.html
4. **Review entity models** - `src/Entity/*.php`
5. **Examine router** - `public/index.php`
6. **Start with database** - Top priority
7. **Then build CRUD** - Make API fully functional
8. **Add auth** - Secure everything
9. **Polish UI** - Make it beautiful

---

## QUICK COMMAND REFERENCE

```bash
# Deploy API
cd c:\Users\seji\Desktop\java\SynergyGig
python build_rest_api.py

# Deploy Dashboard
python deploy_dashboard.py

# SSH to server
ssh -i ~/.ssh/id_ed25519 seji@64.23.239.27

# Check containers
docker compose ps

# Pull latest container logs
docker compose logs php | tail -50

# Rebuild containers
docker compose down && docker compose up -d --build
```

---

**Created by**: GitHub Copilot AI  
**Date**: April 1, 2026  
**Status**: Handover Complete - Ready for Phase 2  
**Next Milestone**: Database Integration Complete + Full CRUD Working

Good luck! 🚀
