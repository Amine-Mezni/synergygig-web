# PROMPT FOR FUTURE AI AGENTS - SynergyGig Backend Development

Use this prompt when you need AI to continue working on SynergyGig:

---

## FULL CONTEXT PROMPT

You are continuing development on **SynergyGig**, an HR & Workforce Management Platform.

### CURRENT STATUS (April 1, 2026)
- ✓ Live web app at http://benzaitsue.work.gd with REST API
- ✓ Docker deployed (Nginx + PHP-FPM on port 80)
- ✓ Web dashboard working at http://benzaitsue.work.gd/dashboard.html
- ✗ Database NOT integrated (MySQL exists but not connected)
- ✗ No authentication system
- ✗ No real data persistence

### IMMEDIATE TASK
**Integrate MySQL database to make API actually store/retrieve data**

### SERVER DETAILS
- **Server**: 64.23.239.27 (Linode VPS)
- **SSH User**: seji
- **SSH Key**: ~/.ssh/id_ed25519 (passphrase: u7@9pd6S##E)
- **Docker**: Running at /home/seji/web_synergygig
- **URL**: http://benzaitsue.work.gd

### DATABASE INFO
- **Type**: MySQL 8.0
- **Database**: finale_synergygig
- **User**: seji
- **Password**: MORTALkombat9pd6S##E
- **Host**: localhost (or from config.properties db.url)

### PROJECT LOCATION
```
Local: c:\Users\seji\Desktop\java\SynergyGig
Server: /home/seji/web_synergygig
Repository: Git clone exists with full history
```

### ENTITIES TO CREATE IN DATABASE
1. **users** - Freelancers, managers, admins
2. **offers** - Job postings with budget & requirements
3. **job_applications** - Users applying to offers
4. **contracts** - Formal agreements between parties
5. **jobs** - Training, HR records (from training_tables.sql)

### SQL FILES AVAILABLE
- `sql/offer_contract_tables.sql` - Contains table schemas
- `sql/training_tables.sql` - HR training tables
- Upload & execute these first to create tables

### API ENDPOINTS WORKING (Test These)
```bash
curl http://benzaitsue.work.gd/api/health
curl http://benzaitsue.work.gd/api/dashboard
curl http://benzaitsue.work.gd/api/offers
```

### PRIORITY ROADMAP

**Phase 1: Database Connection (NEXT)**
1. Create PDO connection class
2. Execute SQL migrations
3. Verify tables exist
4. Test read/write from PHP

**Phase 2: Full CRUD Endpoints**
1. GET /api/offers → Query database
2. POST /api/offers → Insert to database
3. Repeat for users, applications, contracts

**Phase 3: Authentication**
1. Hash passwords (bcrypt)
2. JWT token generation
3. Protect endpoints with middleware

**Phase 4: Validation & Error Handling**
1. Input validation (email, amounts, enums)
2. Proper HTTP response codes
3. Error messages

**Phase 5: Frontend**
1. Login/registration forms
2. Interactive dashboard
3. Job listing with filters

### DEPLOYMENT METHOD
- Use Python + Paramiko for SSH/SFTP
- Existing scripts: `deploy_*.py`, `build_*.py`
- Example: `python build_rest_api.py` uploads & deploys
- Containers auto-restart after upload

### KEY FILES
```
public/index.php          - Router (simple PHP routing)
src/Entity/*.php          - Data models
src/Controller/*.php      - API endpoints
src/Repository/*.php      - Database queries
docker-compose.yml        - Container config
Dockerfile               - PHP 8.3 image
config.properties        - DB credentials
sql/*.sql               - Table schemas
```

### TESTING THE WORK
1. Upload files via Python script using Paramiko
2. SSH to server: `ssh -i ~/.ssh/id_ed25519 seji@64.23.239.27`
3. Check logs: `cd /home/seji/web_synergygig && docker compose logs -f php`
4. Test endpoint: `curl http://benzaitsue.work.gd/api/offers`
5. View dashboard: Open http://benzaitsue.work.gd/dashboard.html in browser

### WHAT TO BUILD NEXT
Based on priority, build in this order:

1. **Database Connection Layer**
   - File: `src/Database/Connection.php`
   - Should: Connect via PDO, run migrations
   - Test: Can query users table

2. **Doctrine ORM Integration** (if using Doctrine)
   - Alternatively: Use PDO directly with repositories
   - Or: Use Doctrine for better ORM

3. **Fully Working Endpoints**
   - All routes return real database data
   - POST/PUT routes actually save data
   - Dashboard shows real stats

Your instructions are in `.instructions.md` - READ IT FIRST.

---

## QUICK START FOR NEW AGENT

1. Read `/memories/session/synergygig_project_state.md` for current state
2. Read `.instructions.md` for full project documentation
3. Check live app at http://benzaitsue.work.gd/dashboard.html
4. Review `src/Entity/*.php` to understand data models
5. Start with database integration (highest impact first)
6. Create Python deployment script to upload new files
7. Test endpoints with curl
8. Verify data persists in database
9. Document what you built
10. Update this prompt for the next agent

---

## COMMON TASKS

### Upload & Deploy New Feature
```python
# In Python script:
import paramiko, tempfile, os

# 1. Connect
client = paramiko.SSHClient()
client.set_missing_host_key_policy(paramiko.AutoAddPolicy())
key = paramiko.Ed25519Key.from_private_key_file(
    os.path.expanduser('~/.ssh/id_ed25519'),
    password='u7@9pd6S##E'
)
client.connect('64.23.239.27', username='seji', pkey=key)

# 2. Upload file
sftp = client.open_sftp()
sftp.put('local_file.php', '/home/seji/web_synergygig/src/Entity/File.php')
sftp.close()

# 3. Restart
stdin, stdout, stderr = client.exec_command(
    "cd /home/seji/web_synergygig && docker compose restart php"
)
client.close()
```

### SSH into Server
```bash
ssh -i ~/.ssh/id_ed25519 seji@64.23.239.27
# Then:
cd /home/seji/web_synergygig
docker compose ps
docker compose logs -f php
```

### Restart Everything
```bash
cd /home/seji/web_synergygig
docker compose down
docker compose up -d
```

---

## RULES TO FOLLOW

1. **Always backup** - Code exist locally first
2. **Test locally** - Review PHP syntax before uploading
3. **Check logs** - `docker compose logs php` after changes
4. **Small steps** - Deploy incrementally, not all at once
5. **Document** - Update .instructions.md when done
6. **Git commit** - Push progress to repository
7. **No hardcoding** - Use config.properties for credentials
8. **Error handling** - Return proper JSON error responses
9. **SQL safety** - Use parameterized queries (PDO prepared statements)
10. **Security** - Hash passwords, validate inputs, use HTTPS soon

---

Created: April 1, 2026 by Copilot  
Status: Ready for Phase 2 (Database Integration)  
Next Agent: Start with database connection + migrations
